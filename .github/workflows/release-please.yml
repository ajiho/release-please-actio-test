on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

name: release-please

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v4
        with:
          release-type: php
          pull-request-title-pattern: "chore: release ${version} 🚀🚀"
          package-name: "release-please-actio-test"
          pull-request-footer: " "